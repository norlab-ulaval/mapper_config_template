# mapper_config_template
A template for the norlab_icp_mapper_ros node config files.

Note that the default mapper config doesn't filter dynamic points and the robot body.
By the end of the [mapping tutorial](https://norlab-icp-mapper.readthedocs.io/en/stable/UsingInRos/), you should be able to filter dynamic points and the robot body.
